import { StyleDirectivDirective } from './style-directiv.directive';

describe('StyleDirectivDirective', () => {
  it('should create an instance', () => {
    const directive = new StyleDirectivDirective();
    expect(directive).toBeTruthy();
  });
});
